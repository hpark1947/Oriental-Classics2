# CLAUDE.md - 프로젝트 규정 및 참고사항

## GitHub 한글 파일명 깨짐 문제 해결

GitHub 웹에서 한글 파일명이 `\354\202\254\353\236\8C` 같은 이스케이프 시퀀스로 표시되는 경우가 있다.
이는 GitHub의 트리 렌더링 캐시 문제로, 새로운 commit을 push하면 캐시가 갱신되어 해결된다.

### 해결 방법
```bash
git commit --allow-empty -m "GitHub 트리 캐시 갱신"
git push
```

### 주의사항
- 이 문제는 파일 자체의 인코딩 문제가 아니라 GitHub 웹 UI의 캐시 문제이다.
- 빈 커밋(empty commit)으로도 캐시가 갱신되므로 코드 변경 없이 해결 가능하다.
- 한글 파일명이 GitHub에서 깨져 보일 때마다 이 방법을 적용한다.

## 작업 완료 후 자동 커밋 & 푸시

작업이 끝나면 별도 지시 없이 자동으로 다음을 수행한다:

1. 변경된 파일을 `git add`로 스테이징한다.
2. 변경 내용을 요약하여 한국어로 커밋 메시지를 작성하고 `git commit`한다.
3. `git push`로 원격 저장소에 푸시한다.

## 발표자료 PPT 보완 규칙

`발표자료/` 폴더의 PPT 파일을 하나씩 보완할 때 다음 원칙을 따른다.

### 1. 배경 색상
- 기존 PPT의 배경이 짙은 파란색·검은색 등 어두운 색으로 되어 있다.
- 모든 슬라이드의 배경을 **무색(흰색/투명)** 으로 변경한다.
- 글자 색도 어두운 배경에 맞춰진 밝은색이라면 가독성 있는 어두운색으로 함께 조정한다.

### 2. 슬라이드 구성: "한 슬라이드 한 주제"
- 한 슬라이드에 너무 많은 내용이 담겨 있어 가독성이 떨어진다.
- **한 슬라이드에는 하나의 주제만** 담는다.
- 청중이 깔끔하고 쉽게 이해할 수 있도록 내용을 분할한다.

### 3. 슬라이드 매수 제약 없음
- **발표 시간이나 슬라이드 개수에 구애받지 않는다.**
- 주제 분할에 필요한 만큼 슬라이드를 늘려도 무방하다.
- 장수(枚數)에 맞추기 위해 한 슬라이드에 내용을 욱여넣지 않는다.

### 적용 순서
- `발표자료/` 폴더 안의 PPT를 사용자가 지시하는 순서대로 하나씩 보완한다.
